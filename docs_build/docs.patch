diff --git a/doc_requirements.txt b/doc_requirements.txt
index d76cefa02..510877332 100644
--- a/doc_requirements.txt
+++ b/doc_requirements.txt
@@ -1,4 +1,3 @@
--r requirements.txt
 plantuml
 sphinx
 sphinx-rtd-theme
diff --git a/docs/Makefile b/docs/Makefile
index dba1b115c..9fe952dab 100644
--- a/docs/Makefile
+++ b/docs/Makefile
@@ -2,7 +2,7 @@
 #
 
 # You can set these variables from the command line.
-SPHINXOPTS           = -W  # turn warnings into errors
+SPHINXOPTS           = # -W  # turn warnings into errors
 SPHINXBUILD          = sphinx-build
 PAPER                =
 BUILDDIR             = _build
diff --git a/docs/conf.py b/docs/conf.py
index af6e0e79e..5c5cba3fb 100644
--- a/docs/conf.py
+++ b/docs/conf.py
@@ -22,14 +22,14 @@ sys.path.insert(0, os.path.abspath('./extensions'))  # noqa
 
 sys.path.insert(0, os.path.abspath('..'))  # noqa
 
-import pulpcore
+#import pulpcore
 
 
 # Set environment variable so Sphinx can bootstrap the Django app
-os.environ["DJANGO_SETTINGS_MODULE"] = "pulpcore.app.settings"
+#os.environ["DJANGO_SETTINGS_MODULE"] = "pulpcore.app.settings"
 
-import django
-django.setup()
+#import django
+#django.setup()
 
 # -- General configuration -----------------------------------------------------
 
@@ -64,9 +64,11 @@ copyright = u'2012-{0}, Pulp Team'.format(date.today().year)
 # built documents.
 #
 # The short X.Y version.
-version = pulpcore.__version__
+#version = pulpcore.__version__
+version = 'DRAFT'
 # The full version, including alpha/beta/rc tags.
-release = pulpcore.__version__
+#release = pulpcore.__version__
+release = 'DRAFT'
 
 # The language for content autogenerated by Sphinx. Refer to documentation
 # for a list of supported languages.
